---
name: run-next-ticket
description: Pick the next pending ticket from the local .matt/ workspace based on filename prefix, then drive it to completion via /tdd and /finalize. Use when user says "run next ticket", "next ticket", "/run-next-ticket", or wants to start the next piece of work in an in-progress feature.
---

# Run Next Ticket

Pick the next executable ticket from `.matt/tickets/` and drive it to completion.

> **Note:** `/run-all-tickets` no longer calls `/run-next-ticket`. It selects each ticket itself, dispatches a subagent that runs `/tdd` + `/code-review` directly, and owns the independent external review, `done-` rename, and commit in its own main loop. This skill remains for standalone, single-ticket human use — its behavior below is unchanged.

## Preconditions

Current working directory must contain a `.matt/` workspace with:
- `tickets/NN-<slug>.md` files (numbered from 01; completed ones prefixed `done-`)
- `CLAUDE.md` (generated by `/load-feature`)

`SPEC.md` is optional.

If preconditions are missing, stop and tell the user to run `/load-feature <slug>` first. Do not try to create or recover them.

## Status convention

Ticket completion status is encoded in the filename:
- Pending: `NN-<slug>.md`
- Done:    `done-NN-<slug>.md`

Filename is the single source of truth. Do not look at commit history or any `Status:` field inside the file.

## Process

### 1. Load workspace rules

Read `.matt/CLAUDE.md` explicitly. (Subdirectory CLAUDE.md is lazy-loaded; reading it now ensures the sync rules are in context before any subsequent file modification.)

### 2. Load tickets

List `.matt/tickets/*.md`. For each capture:
- File path
- Number `NN` (digits after an optional `done-` prefix)
- Title (first `#` heading; fallback to filename slug)
- Done flag (filename starts with `done-`)
- `Blocked by` references parsed via the **deterministic Blocker parsing** rules defined in `/run-all-tickets` (see its `## Blocker parsing (deterministic)` section). Same rules apply here for parity: bullets must be backtick-wrapped `` `NN-slug.md` `` or hash `#NN` form; `None` / no section means no blockers; anything else fails fast with the same `❌ Ticket <NN> has unparseable Blocked by line: <line>` message. Capture the extracted `NN` list.

### 3. Pick the next ticket

Among pending tickets (no `done-` prefix):
1. Drop any whose `Blocked by` numbers are not all done.
2. From the remaining, pick the one with the **smallest number**.
3. If none remain (all done, or all pending blocked), say so clearly and stop.

### 4. Report and hand off

Print:
- Selected ticket: path, number, title
- One-line reason (e.g. "smallest pending; blockers [01] done")

Then immediately invoke `/tdd` with the selected ticket file path as input. Tell the TDD flow:

> Implement the ticket at `<path>`. The ticket file is the full contract — acceptance criteria are inside.
> When you judge the ticket fully complete, invoke `/finalize` with the ticket file path (`/finalize <path>`) so the
> external reviewers verify the implementation against this ticket's acceptance criteria.
> Do NOT push. Do NOT rename or move the ticket file — this skill (`/run-next-ticket`) handles that in its own step 5 after `/finalize` returns.

### 5. Mark done

After `/finalize` returns with a passing summary, **you (the `/run-next-ticket` orchestrator) must explicitly rename the ticket file**:

```bash
git mv .matt/tickets/<NN>-<slug>.md .matt/tickets/done-<NN>-<slug>.md
```

This step is mandatory and is the single source of truth for ticket completion. Do NOT skip it, do NOT delegate it to `/tdd` or `/finalize` (they will not do it — `/finalize`'s summary is a terminal output and forbids trailing actions). Do NOT commit; the caller (`/run-all-tickets` or the user) owns commits.

If `/finalize` does not pass (failures it logs as won't-fix don't count as a pass) → do NOT rename; report the failure and stop.
